# assets

Images referenced by the main README.

Drop these two screenshots here (exact filenames — the README already points at them):

| File | What to capture |
|------|-----------------|
| `apikey-create.png`      | The ElevenLabs **API Keys** page with the **Create API Key** button |
| `apikey-permissions.png` | The key's **permissions** view with **Speech to Text** enabled (the important one) |

Once both files are in place, the README's image references will be un-commented so
they render on GitHub. PNG or JPG is fine; keep them a reasonable width (~1000px).
